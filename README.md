# Insta_bot_selenium
This is a 2 part Instagram bot built using selenium, so it works on the website version of Instagram
Instagram updates its site code periodically, so the tag id and tag class might change
But the general use of xpath will remain same
